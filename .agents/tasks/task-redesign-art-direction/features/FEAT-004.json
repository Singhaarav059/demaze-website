{
  "id": "FEAT-004",
  "type": "fix",
  "description": "Responsive behavior + animation timing/easing smoothness pass across all breakpoints, closing out the redesign. Verifies and fixes mobile/tablet layouts (the >=901, 901-down, and 600-down media blocks in src/styles.css) so nothing overflows, is cut off, is awkwardly zoomed, or breaks; and ensures animation timing/easing feels fluid and premium and cohesive. CSS-only (src/styles.css). Builds on FEAT-002 and FEAT-003. Adapt test's 'subtle intensity' motion philosophy (short travel, gentle easing) to main's existing effects; do NOT make motion flashier.",
  "status": "completed",
  "steps": [
    "TABLET (max-width:900px block ~line 1927): verify home-hero/about-split/contact-layout/resource/download collapse to 1 column cleanly; the ScrollFocusStack chapters correctly drop their sticky/transform (they already reset to position:relative/top:auto/transform:none) so no chapter is stuck or clipped; pinned-services collapses to the static 14rem visual + full list (with FEAT-002's contain fit); service-grid goes 2-col; values/process/industry go 2-col. Fix any element that overflows the viewport width (check .service-card width:94% offsets, .stack-chapter width calc, tech marquee mask).",
    "PHONE (max-width:600px block ~line 2040): verify hero h1 clamp(2rem,9.4vw,2.75rem) does not overflow; hero-actions 2-col grid buttons do not clip labels; metrics-strip 2x2 with correct borders; all grids single-column; the industries inline-flow list uses the new middot separator from FEAT-003 and does not run words together; service cards single column with alternating slight offsets not causing horizontal scroll; founder-story single column image min/max-height sane; footer-cta and footer-main reflow; contact-form single column. Fix any horizontal overflow (body has overflow-x:hidden but elements should still fit).",
    "VERIFY no fixed/absolute element (scroll-progress bar, consent-banner, video-control, chapter-doodle, service-grid::before glow) escapes its intended bounds on small screens; chapter-doodle already scales 0.72 at <=600 - confirm it does not overlap heading text.",
    "ANIMATION TIMING/EASING: audit every transition/animation duration and easing for a cohesive, premium, subtle feel using the existing var(--ease-out) cubic-bezier(0.22,1,0.36,1) where appropriate. Ensure: scroll-reveal (0.65s/0.8s ease-out) is consistent; tech-marquee 26s linear is smooth and seamless; the lerp factors in scroll-focus-stack (0.12), services grid (0.11), pinned-services (0.14) produce smooth non-janky motion; hover-lifts (service-card 0.3s, contact-options 0.25s, project-card image 0.6s) are consistent in feel. Do NOT change the JS lerp constants unless one is demonstrably janky; prefer harmonizing CSS transition durations/easings. Keep all of it gated by prefers-reduced-motion (the global @media reduce block at ~line 2224 already neutralizes animations/transitions and scroll-reveal - confirm new rules are covered by it).",
    "Confirm the prefers-reduced-motion:reduce block still fully neutralizes any transition/animation added or changed in FEAT-002/003/004 (it uses a universal `*` selector so new rules are covered, but verify nothing sets an animation via inline style that bypasses it - the JS components already early-return under reduced motion).",
    "Final consistency sweep: load-order sanity of media queries (base -> min-width:901 -> max-width:900 -> max-width:600 -> reduce) is preserved so later breakpoints correctly override earlier ones."
  ],
  "acceptance_criteria": [
    "At 1200+, 901-1199, 600-900, and <600 widths, no element overflows the viewport horizontally, is cut off, awkwardly zoomed, or breaks layout (verified by reading the media blocks and reasoning about each grid; final confirmation deferred to preview).",
    "The industries inline list on phone shows a visible separator between solutions (not run-together words).",
    "ScrollFocusStack chapters are non-sticky and fully visible on <=900px; pinned-services shows a properly contained static visual + full readable list on mobile.",
    "All animations/transitions use cohesive durations/easings and are fully neutralized under prefers-reduced-motion:reduce.",
    "src/styles.css is valid CSS with media queries in an order where later/narrower breakpoints override earlier ones; no :root token redefined; site-data.ts unchanged."
  ],
  "verification": [
    "Deferred to CI: `unset NODE_OPTIONS && npm run typecheck && npm run lint && npm test && npm run build` all pass.",
    "Read the three media-query blocks end to end and confirm brace balance and override order.",
    "Confirm `git diff --stat` shows changes confined to src/styles.css (plus at most the projects-showcase-grid.tsx SPANS array from FEAT-003)."
  ],
  "blocked_reason": null,
  "findings": "CSS-only pass (src/styles.css); no .tsx/data files touched (git diff confined to src/styles.css + this feature JSON). Built on FEAT-002/003 without reverting them. CONCRETE GRID FIX: added `grid-auto-flow: dense;` to .project-showcase-grid (~line 973). FEAT-003 varied the SPANS to four 2x2 large tiles among 16 in a 4-col 220px-auto-row grid with NO dense flow, so auto-placement left empty gaps whenever a 2x2 couldn't fit the remaining columns of a row - the exact 'excessive empty space / randomly positioned' defect. Dense lets 1x1 tiles backfill the holes so the wall is gap-free; visual order shifts slightly (acceptable for a decorative portfolio wall, numbered captions still identify each project); harmless at the <=900px 2-col variant where .project-showcase-card resets grid-area:auto. ANIMATION/EASING HARMONIZATION toward a cohesive subtle-premium feel using var(--ease-out) cubic-bezier(0.22,1,0.36,1): (1) .animate-rise 0.75s and .animate-visual 0.95s converted from the off-brand cubic-bezier(0.2,0.75,0.25,1) to var(--ease-out); (2) .project-card image hover 0.6s converted from cubic-bezier(0.2,0.75,0.25,1) to var(--ease-out); (3) site-header condense transition (background/box-shadow/backdrop-filter 0.3s), desktop-nav underline (right, bumped 0.2s->0.25s), pinned-services list h3 color 0.3s + description reveal (max-height 0.4s/opacity 0.3s), service-grid glow opacity 0.3s, service-card border/shadow/bg 0.25s, and contact-options hover-lift (transform/box-shadow 0.25s) all switched from plain `ease` to var(--ease-out). LEFT UNTOUCHED: .scroll-reveal (already 0.65s/0.8s var(--ease-out)), .service-live-visual transform (already 0.55s var(--ease-out)), tech-marquee 26s linear infinite (linear is correct for a seamless -50% loop over a doubled track), ambient/float/orbit/scan/ping looping keyframes (ease-in-out infinite - correct for ambient loops), the skip-link 0.2s ease (a11y focus affordance), and ALL JS lerp constants (scroll-focus-stack 0.12, services grid 0.11, pinned-services 0.14 - none demonstrably janky). RESPONSIVE VERIFICATION (read all three cascade blocks end to end; order base -> min-width:901 -> max-width:900 -> max-width:600 -> reduce preserved so narrower breakpoints override wider): TABLET (<=900) home-hero/about-split/contact-layout/resource-hero/download-layout collapse to 1col; ScrollFocusStack .stack-chapter/.motion-chapter/.motion-chapter-long reset to position:relative/top:auto/transform:none/opacity:1/filter:none with motion-stage min-height:0 (no chapter stuck/clipped); pinned-services collapses to static single-col (14rem contain visual from FEAT-002) + full list; service-grid 2col; values/process/industry 2col. PHONE (<=600) hero h1 clamp(2rem,9.4vw,2.75rem) fits; hero-actions 2col grid with menu-toggle/tap targets >=2.75rem(44px) and button font 0.72rem full-width so labels don't clip; metrics-strip 2x2 with nth-child(3)/(4) border-top and nth-child(3) border-left:0; all grids single-column; industries inline-flow list uses the middot separator (content:\"\\00B7\") added in FEAT-002 with margin-left 0.3rem so words don't run together; service cards single column width:94% with alternating +-0.25rem justify-self shifts (no horizontal scroll - body has overflow-x:hidden and .site-page overflow:clip as backstop); founder-story single col img min-height:17rem/max-height:22rem; footer-cta/footer-main/footer-base reflow; contact-form single column. FIXED/ABSOLUTE elements verified in-bounds on small screens: .scroll-progress (fixed top/left, JS-driven width, 3px), .video-control (absolute inside rounded .hero-visual, right/bottom 0.8rem, min 2.75rem tap target), .consent-banner (fixed, width:min(31rem,calc(100% - 2rem))), .chapter-doodle (absolute far-top-right 5rem x 2.6rem @ opacity 0.5, scales 0.72 with right:0.25rem/top:0.4rem at <=600 - sits in the corner clear of the left-aligned headings), .service-grid::before glow hidden at <=600. REDUCED MOTION: the @media(prefers-reduced-motion:reduce) universal-selector block (animation:none!important + transition-duration:0.01ms!important on *, *::before, *::after) neutralizes every transition/animation including all harmonized ones; .scroll-reveal forced opacity:1/transform:none; tech-band-track animation:none; JS motion components early-return under reduced motion. In-sandbox correctness checks: braces balanced 398/398, no bare `content:;`, no :root design token redefined, no line >100 cols (prettier printWidth). Full verification (typecheck/lint/test/build + device scroll-smoothness + reduced-motion pass) deferred to CI/preview per INTEGRATIONS_ONLY constraint (npm registry unreachable, node_modules absent)."
}
