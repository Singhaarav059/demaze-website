{
  "id": "FEAT-003",
  "type": "fix",
  "description": "Fix concrete performance, UX, and accessibility issues in the interactive components, without introducing risky architectural changes (branch must stay working; syncs to Lovable). Three targeted problems: (1) the hero video keeps decoding a looping video while scrolled off-screen; (2) the PinnedServicesShowcase is effectively hover-driven with poor touch discoverability and its stacked preview images all expose real alt text so screen readers announce four overlapping descriptions for one visible image; (3) confirm the multiple simultaneous scroll listeners are each reduced-motion guarded and rAF-gated (they are) and make no regressions. All changes must preserve prefers-reduced-motion behaviour.",
  "status": "completed",
  "steps": [
    "src/components/hero-video.tsx: pause the <video> when it scrolls out of view and resume when it returns (respecting the existing reduced-motion + playing state logic and the manual pause toggle). Use an IntersectionObserver on the video/container: when not intersecting, call video.pause(); when intersecting again and reduced-motion is not set and the user has not manually paused, resume play(). Do not fight the existing user-facing play/pause button — track whether the pause was user-initiated vs visibility-initiated so the manual state is respected. Clean up the observer in the effect's return. Keep the existing scroll-tilt rAF logic.",
    "src/components/pinned-services-showcase.tsx: improve touch/keyboard discoverability and fix a11y. (a) The four stacked preview <img> in .cinema-services-visual-wrap are decorative duplicates layered by opacity; give the non-active images empty alt (alt=\"\") and aria-hidden, or render alt only for the active image, so assistive tech announces one image, not four. (b) Each row is role=\"button\" with tabIndex=0 and Enter/Space handlers — verify keyboard activation works and add a visible focus style hook if none exists (coordinate with styles.css: the .cinema-service-item has hover/is-active styles at ~line 2806; ensure :focus-visible gets the same active treatment so keyboard users see the reveal). (c) Consider making the reveal also respond to focus (onFocus setActiveIdx) so keyboard tab-through reveals each service body, matching the hover behaviour.",
    "In src/styles.css, add a :focus-visible rule for .cinema-service-item that mirrors the .is-active / hover visual treatment (background + title color) so keyboard focus is clearly visible. Match the existing OKLCH color-mix pattern used at .cinema-service-item.is-active (~line 2817). Do not restructure unrelated CSS.",
    "Review (do not necessarily change) the set of scroll listeners across scroll-focus-stack.tsx, content-sections.tsx (ServicesGrid), scroll-progress.tsx, projects-showcase-grid.tsx, and site-shell.tsx: confirm each is passive, rAF-gated, and reduced-motion guarded. If any listener is found NOT reduced-motion guarded or NOT cleaned up on unmount, fix it. Record findings; do not merge them into a shared controller (too risky for this branch)."
  ],
  "acceptance_criteria": [
    "hero-video.tsx pauses the video when off-screen and resumes when back on-screen, respects prefers-reduced-motion, respects a user-initiated pause (does not auto-resume something the user paused), and disconnects its IntersectionObserver on cleanup.",
    "In pinned-services-showcase.tsx, only one image description is exposed to assistive tech at a time (inactive stacked images have alt=\"\" and/or aria-hidden).",
    ".cinema-service-item has a visible :focus-visible state in styles.css matching its active treatment, and keyboard focus reveals the active service (via onFocus or existing key handling).",
    "All scroll listeners remain passive, rAF-gated, reduced-motion guarded, and cleaned up on unmount; any that were not are fixed.",
    "Edits match existing Prettier/ESLint formatting so lint stays clean."
  ],
  "verification": [
    "Read hero-video.tsx and confirm an IntersectionObserver pauses/resumes with reduced-motion and manual-pause guards plus cleanup",
    "Read pinned-services-showcase.tsx and confirm inactive images are hidden from a11y and focus reveals the service",
    "grep styles.css for a .cinema-service-item:focus-visible rule",
    "In CI: npm run typecheck, npm run lint, npm test, npm run build all pass"
  ],
  "blocked_reason": null,
  "findings": "hero-video.tsx: added an IntersectionObserver (threshold 0.1) on the <video> that pauses when off-screen and resumes when back on-screen, guarded by both reducedMotion.matches and a new userPausedRef. The toggle button now sets userPausedRef.current (true on manual pause, false on manual play) so visibility changes never auto-resume a video the user deliberately paused. Observer is disconnected in the effect cleanup. Existing scroll-tilt rAF logic and the play/pause button are unchanged. pinned-services-showcase.tsx: inactive stacked preview images now get alt=\"\" plus aria-hidden={true}, so only the active image exposes a description to assistive tech; also added onFocus={() => setActiveIdx(i)} to the rows so keyboard tab-through reveals each service body like hover does. styles.css: added .cinema-service-item:focus-visible sharing the .is-active color-mix background, plus a 2px accent outline with outline-offset, and included :focus-visible in the title-color rule so the focused title matches the active/hover accent color. Step 4 (scroll listener review): reviewed all five listeners; ALL were already passive + rAF-gated + reduced-motion guarded + cleaned up on unmount, so NO changes were needed. Specifics: scroll-focus-stack.tsx (guards reducedMotion+compactLayout in paint/measure, cancels both frames on cleanup); content-sections.tsx ServicesGrid (guards reducedMotion+compactLayout in paint, disconnects IntersectionObserver + cancels frames on cleanup); scroll-progress.tsx (early-return on reducedMotion, cancels frame on cleanup); projects-showcase-grid.tsx (early-return on reducedMotion, cancels frame on cleanup); site-shell.tsx header condense (early-return on reducedMotion, cancels frame on cleanup). Did NOT merge them into a shared controller per instructions. ENV: could not run typecheck/lint/test/build locally (INTEGRATIONS_ONLY, npm registry blocked, no node_modules binaries) — edits made type-correct and lint-clean by construction, matching existing Prettier formatting; must be validated in CI. FOLLOW-UP (review v1, 2026-09-10-172633-review.md): the reduced-motion apply() resume path was gated only by userPausedRef + reducedMotion.matches, so toggling reduced motion off while the hero was scrolled off-screen started playback off-screen until the next scroll re-triggered the observer. Fixed by tracking the latest on-screen state in an effect-scoped `isIntersecting` variable that the IntersectionObserver updates; apply()'s resume path now also requires `isIntersecting`, making the observer the single source of truth for visibility. Initialised to true so initial mount autoplay is preserved (hero is on-screen then); observer corrects it on its first callback. All prior behaviour (initial autoplay, reduced-motion pause, user-initiated pause, observer cleanup, scroll-tilt rAF) is unchanged."
}
